{
  "name": "finnances-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && ts-node tests/crearTransaccion.test.ts && ts-node tests/calcularSaldo.test.ts",
    "start": "node dist/index.js",
    "prisma": "prisma"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@prisma/client": "^5.15.0",
    "fastify": "^4.23.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prisma": "^5.15.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
